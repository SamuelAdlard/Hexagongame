using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Graveyard : MonoBehaviour
{/*
    // Start is called before the first frame update
    void Create()
    {
        //Instantiate(Grass, Groundplacement, Quaternion.identity);
    }
    void Start()
    {
        
    }
    void Tileplacement()
    {
        if (Start1 == false)
        {
            CancelInvoke();
            Destroy(this);
        }
        else
        {

            Vector3 Up = new Vector3(-1.72f, 0, 0) + transform.position;
            Vector3 Down = new Vector3(1.72f, 0, 0) + transform.position;
            Vector3 UpLeft = new Vector3(-0.86f, 0, 1.5f) + transform.position;
            Vector3 DownLeft = new Vector3(0.86f, 0, 1.5f) + transform.position;
            Vector3 UpRight = new Vector3(-0.86f, 0, -1.5f) + transform.position;
            Vector3 DownRight = new Vector3(0.86f, 0, -1.5f) + transform.position;
            float Sides = Random.Range(1, 6);
            int Side = Mathf.RoundToInt(Sides);
            if (Side == 1)
            {
                Groundplacementtest = Up;
            }
            if (Side == 2)
            {
                Groundplacementtest = Down;
            }
            if (Side == 3)
            {
                Groundplacementtest = UpLeft;
            }
            if (Side == 4)
            {
                Groundplacementtest = DownLeft;
            }
            if (Side == 5)
            {
                Groundplacementtest = UpRight;
            }
            if (Side == 6)
            {
                Groundplacementtest = DownRight;
            }
            SideCheck();
        }

    }
    void SideCheck()
    {

    }
    // Update is called once per frame
    void Update()
    {
        
    }
*/}
